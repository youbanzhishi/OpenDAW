"""
ai — AI composition, smart mixing, arrangement-mixing, transcription,
     reference matching, style transfer, and remix (Phase 15-17).

Modules:
    music_theory       — Scales, chords, chord progressions, key detection
    composer           — AI composition engine
    smart_mixer        — Smart mixing closed-loop
    arrangement_mixer  — One-click compose + mix
    transcription      — AI扒带 pipeline (Phase 17)
    reference_matcher_v2 — Reference style matching (Phase 17)
    style_transfer     — Style transfer engine (Phase 17)
    remix              — One-click remix engine (Phase 17)
"""

from __future__ import annotations
